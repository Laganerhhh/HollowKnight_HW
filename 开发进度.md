## HollowKnight_HW 开发进度

### 当前阶段结论

项目已经完成 **Lua UI Panel 化迁移基础框架** 与 **Addressables 远端 Lua 热更闭环**，当前不是从零开始迁移 UI，也不是停留在热更可行性验证阶段。

目前已经验证：

- **Lua 文件可以作为 Addressables 远端资源下载**。
- **下载后的 Lua 文件会写入 `Application.persistentDataPath` 下的热更目录**。
- **ToLua 会优先从热更目录读取 Lua 文件**。
- **本地 `Assets/Lua` 只作为兜底 Lua 源**。
- **远端修改 Panel Lua 后，重新启动游戏可以看到远端逻辑生效**。
- **`Main.lua` 中的 `VERSION` 字段已经用于标识 Lua 版本**。
- **已成功打印远端 Lua 版本日志，例如：`[LuaHotUpdateTest] Lua 版本0.0.1`**。

### Lua UI Panel 迁移状态

原始 C# UI 迁移到 Lua Panel 的工作已经完成了一批，不是待开始状态。

当前已具备：

- **已迁移 5 个 UI 到 Lua Panel**。
- **Lua UI 生命周期框架已经完成**。
- **Lua 侧 `UIPanelManager` 已经完成并能正常运行**。
- **C# 原始 UI 已经可以被 Lua 控制逻辑替换**。
- **示例 Panel 可参考 `Assets/Lua/View/StartPanel.lua`**。
- **基础生命周期可参考 `Assets/Lua/View/BasePanel.lua`**。
- **面板管理逻辑可参考 `Assets/Lua/Logic/UIPanelManager.lua`**。

当前 Lua Panel 生命周期约定包括：

- `Ctor`
- `InitUIAndMetaData`
- `InitUIEvent`
- `OnOpen`
- `RefreshView`
- `OnHide`
- `OnDispose`

`BasePanel` 当前已经负责：

- 创建和挂载 Panel Prefab。
- 注入 Panel definition。
- 注入 UI 上下文。
- 初始化 UI 节点和事件。
- 显示、隐藏和释放 Panel。
- 支持缓存型与非缓存型 Panel。

`UIPanelManager` 当前已经负责：

- 注册 Panel 定义。
- 创建 Panel 实例。
- 打开、关闭、返回、销毁 Panel。
- 管理不同 UI Layer。
- 管理 Panel 栈。
- 驱动可见 Panel 的 `OnUpdate`。
- `DisposeAll` 清理全部 Panel。

### Lua 热更实现状态

当前采用的热更策略是：

**启动前下载并落地 Lua 文件，之后启动 Lua VM 时优先加载热更目录，下次启动或本次 Lua VM 启动前即可生效。**

当前暂未实现运行时动态 reload，也就是暂未做完整的 `package.loaded` 清理与运行中 Panel 替换。

已完成内容：

- **`LuaConst.cs` 已明确热更 Lua 根目录相关配置**。
- **`LuaState.cs` 已调整 Lua 搜索路径优先级**，优先读取热更目录。
- **`LuaHotUpdateManager.cs` 已实现 Lua 文件下载和落地流程**。
- **`LuaManifest.json` 已作为 Lua 热更清单使用**。
- **`LuaManifest.version` 已加入，用于记录远端 Lua 版本**。
- **Editor 同步工具已自动生成 `LuaManifest.json` 并配置 Addressables**。

### Addressables Lua 热更配置状态

已经完成 Addressables 自动配置工具，避免手动设置 Key 出错。

工具菜单：

```text
Tools/Lua HotUpdate/Sync Lua To LuaHotUpdate
```

工具当前负责：

- 扫描 `Assets/Lua` 下的 `.lua` 文件。
- 同步到 `Assets/LuaHotUpdate`。
- 将 Lua 文件转为 `.lua.bytes`。
- 生成 `LuaManifest.json`。
- 从 `Main.lua` 读取 `VERSION` 并写入 `LuaManifest.version`。
- 自动将 Lua 文件加入 Addressables。
- 自动设置 Address，例如：
  - `Lua/LuaManifest.json`
  - `Lua/Main.lua`
  - `Lua/View/StartPanel.lua`
- 自动设置 `lua` label。
- 清理旧的 `Assets/LuaHotUpdate` 文件夹级 Addressables 入口，避免 key 错误。

注意事项：

- 修改 `Assets/LuaHotUpdate/*.bytes` 后，如果再次执行同步工具，会被 `Assets/Lua` 中的源 Lua 覆盖。
- 正式修改版本时，应优先修改 `Assets/Lua`，再执行同步工具和 Addressables Build。
- 远端部署时需要上传 `catalog_1.0.json`、`catalog_1.0.hash` 以及变化的 `.bundle`。
- 初次验证时可直接覆盖上传整个 `ServerData/StandaloneWindows64` 目录。

### Lua 下载 UI 增强状态

已经完成启动前 C# 控制的 Lua 更新界面。

Prefab：

```text
Assets/Prefab/UI/LuaUpdateUI.prefab
```

控制脚本：

```text
Assets/Scripts/Resource/LuaUpdateUIView.cs
```

该 UI 复用了之前 `DownPanel.prefab` 的节点结构，只改了 prefab 名称。

当前约定节点包括：

- `progressBar/progress`
- `progressPercentText`
- `downloadSizeText`
- `downloadSpeedText`
- `statusTip`
- `retryBtn`

当前实现特点：

- **不需要手动放入场景**。
- **不强制手动挂脚本到 prefab 根节点**，运行时会自动补充 `LuaUpdateUIView`。
- **`LuaHotUpdateManager` 会在 Lua VM 启动前加载 Addressables 地址 `UI/LuaUpdateUI`**。
- **运行时会自动创建临时 Canvas 和 EventSystem**。
- **Lua 下载、加载 Manifest、安装 Lua 文件、失败重试等状态会显示在这个 C# UI 上**。
- **UI 文本全部使用英文**。

### 缓存行为说明

测试时观察到：删除 `persistentDataPath/Win/Lua` 后，下次运行 Lua 下载大小可能显示为 `0`。

这是正常现象，原因是：

- 删除 `Win/Lua` 只删除了已经落地的 Lua 文件。
- Addressables 自身还有 Unity 缓存。
- 如果 bundle 已经被 Addressables 缓存，再次加载时可能不需要重新网络下载。
- 因此下载大小为 `0` 不代表热更失败。

如果需要完整模拟首次下载安装，需要同时清理：

- `persistentDataPath` 下的 Lua 热更目录。
- Unity Addressables / AssetBundle 缓存。

### 最新迁移进展

已开始继续迁移更多原始 C# UI 到 Lua。

本轮已迁移：

- **`TutorialUI` 已迁移为 Lua 逻辑控制**。
- **新增 `Assets/Lua/View/TutorialPanel.lua`**。
- **`Assets/Scripts/UI/TutorialUI.cs` 已改造成兼容桥接层**：
  - 保留 `TutorialUI.instance`。
  - 保留 `ShowTutorial(TutorialUITyepe type, float displayTime)`。
  - 保留 `HideTutorial(TutorialUITyepe type)`。
  - 优先转发到 Lua 的 `TutorialPanelBridge.ShowTutorial/HideTutorial`。
  - Lua 未就绪时继续走 C# 兜底显示逻辑。
  - 自动隐藏计时仍由 C# 负责，避免依赖未知 Lua coroutine wait API。
  - 已加入 token 机制，避免旧延时隐藏协程误隐藏后续新提示。
- **`Assets/Lua/Logic/PanelRegistry.lua` 已注册 `TutorialPanel`**，并在注册表加载阶段显式 `require "View.TutorialPanel"`，确保 C# 桥接函数提前存在。
- **`TutorialPanel` 当前是场景实例绑定型 Panel**，不会重新实例化 `TutorialUI.prefab`，而是绑定场景中已有的 `TutorialUI` GameObject。
- **`TutorialUI.prefab` 已被多个关卡场景引用**，因此当前迁移策略避免直接修改场景或重复创建第二份 UI。

本轮迁移后的验证重点：

- 进入触发区域时，控制台应打印 `TutorialPanel.ShowTutorial`。
- 离开触发区域时，控制台应打印 `TutorialPanel.HideTutorial`。
- 玩家受伤后回血提示应正常显示。
- 回血后 Recover 提示应正常隐藏。
- 场景切换后提示 UI 不应引用旧场景对象。

### 下一步建议

当前不建议立即优先实现完整运行时 reload。

原因：

- `package.loaded` 清理只能影响后续 `require`，不能自动替换已存在的 Panel 实例。
- 已打开 UI 的事件监听、闭包、协程、Timer、C# delegate 可能仍引用旧 Lua 对象。
- 核心模块如 `Main`、`BasePanel`、`UIPanelManager` 不适合贸然运行时 reload。

推荐下一步方向：

1. **继续完善已迁移的 Lua Panel 的实际业务逻辑**。
2. **继续迁移剩余 C# UI 到 Lua Panel**。
3. **沉淀统一事件注册和注销规范**，为之后运行时 reload 做准备。
4. **当已有多个 Panel 生命周期稳定后，再实现受控的 Panel 级 reload**。

未来运行时 reload 推荐策略：

- 只 reload 指定 Panel。
- 先关闭或销毁旧 Panel 实例。
- 注销旧事件监听。
- 清理对应模块的 `package.loaded`。
- 重新 `require` Panel 模块。
- 重新打开 Panel。

不建议一开始做全量 Lua reload。

### 最新架构修正（Lua VM 常驻与场景化启动 UI）

为解决 **进入 `Level1` 后 Lua 虚拟机被卸载** 以及 **在 `Level1` 手动补 `LuaClient` 后又弹出 `StartPanel`** 的问题，已完成以下架构修正：

- **删除了 `Level1` 中额外的 `LuaRoot`**，避免关卡场景重复启动第二套 Lua VM。
- **`Assets/ToLua/Misc/LuaClient.cs` 已改为单例防重 + `DontDestroyOnLoad(gameObject)` 常驻**：
  - 如果场景中再次出现第二个 `LuaClient`，会直接销毁重复实例。
  - 从 `StartMenu` 切换到 `Level1` 时，原有 Lua VM 会继续保留。
- **`Assets/Lua/Controller/UIRootCtrl.lua` 已改为按场景处理 UI，而不是启动时无条件打开 `DownloadPanel/StartPanel`**：
  - `Start()` 只负责初始化 `UIPanelManager` 和注册 `PanelRegistry`。
  - 根据当前激活场景名决定是否打开启动场景 UI。
  - 只有在 `StartMenu` 场景才会打开 `DownloadPanel` / `StartPanel`。
  - 在 `Level1` 等非启动场景中，不再自动弹出主菜单 UI。
- **场景切换时会销毁场景绑定型 Panel 实例**，避免跨场景持有旧的 `UIManager`、旧节点或旧场景对象引用。

本轮修正后的直接收益：

- **`PausePanel` 所依赖的 Lua VM 不会在切到 `Level1` 时丢失**。
- **直接从启动场景进入关卡后，按 `ESC` 可以继续走 Lua PausePanel 流程**。
- **即使未来直接运行 `Level1` 做联调，也不会因为 `UIRootCtrl` 启动逻辑而自动弹出 `StartPanel`**。

本轮修正后的验证重点：

- 从 `StartMenu` 进入 `Level1` 后，控制台仍应能看到 Lua 侧场景切换日志。
- 进入 `Level1` 后不应再自动显示 `StartPanel` 或 `DownloadPanel`。
- 在 `Level1` 按 `ESC` 时，应暂停游戏并正常创建 `PausePanel`。
- 回到 `StartMenu` 时，应重新显示启动场景对应的 Lua UI，而不是残留关卡中的面板实例。

------

## [2026-09-02 15:24] - 修复返回主菜单后二次进入关卡无法暂停

- 修改内容：更新 `Assets/Scripts/Game/GameManager.cs`，为常驻 `GameManager` 增加 `SceneManager.sceneLoaded` 监听，在场景加载完成后统一重置 `isPlaying = true` 并重新绑定当前场景的 `Player/Animator`；无数据库变更，无API变更。
- 影响范围：修复从 `PausePanel` 返回主菜单后再次进入 `Level1` 时，按 `ESC` 不响应、无法暂停和无法打开 `PausePanel` 的问题。
- 状态：已完成
- 待办提醒：建议联调验证 `PausePanel -> 返回主菜单 -> StartPanel -> Level1 -> ESC` 整条链路，并留意主菜单场景下是否需要额外忽略 `ESC` 输入。

------

## [2026-09-02 15:53] - 新增GM测试工具（无敌与快速切关）

- 修改内容：新增 `Assets/Scripts/GM/GMTool.cs`，运行时自动创建常驻 GM 调试器，支持 `F1` 切换无敌、`F2` 快速进入下一关；更新 `Assets/Scripts/Player/PlayerHealth.cs`，在 `TakeDamage(...)` 入口接入 `GMTool.IsGodModeEnabled` 统一拦截伤害；无数据库变更，无API变更。
- 影响范围：测试时无需手动放置场景对象即可快速验证战斗、生存和关卡跳转流程。
- 状态：已完成
- 待办提醒：建议联调验证 `F1` 开关无敌后敌人碰撞/陷阱/掉落是否都不扣血，以及 `F2` 在最后一个 Build Settings 场景时是否正确提示不可继续切关。

------