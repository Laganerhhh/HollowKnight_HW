# HollowKnight_HW 技术总结

## 1. 概述

这一阶段，项目在原有玩法和关卡基础上，重点补齐了工程化能力。核心新增内容包括：

- 资源管理系统
- 资源热更新（含下载）
- ToLua 热更新
- 基于 Lua 的 UI 开发框架
- 怪物 / 特效 / 投射物对象池
- 存档系统
- 一批与真机运行相关的稳定性修复

如果说之前的项目重点在于“玩法实现”，那么这一阶段的重点就是“让项目更容易维护、更容易扩展、更接近完整游戏工程”。

## 2. 资源管理系统

### 2.1 设计目标

资源系统的主要目标有三个：

1. 为上层业务提供统一的资源加载入口
2. 隔离底层 Addressables 的具体实现细节
3. 为后续远端下载、缓存控制、热更新提供基础设施

项目中最终采用了两层结构：

- `ResourceManager.cs`：上层门面，负责统一封装常用资源接口
- `BundleManager.cs`：底层执行者，负责 Addressables 初始化、加载、释放、实例化与远端下载

### 2.2 统一资源入口

`ResourceManager` 的思路很直接：业务层不直接接触复杂的 Addressables API，而是统一走简化接口。

例如：

```csharp
public T Load<T>(string path) where T : UnityEngine.Object
{
    return BundleManager.EnsureInstance().LoadAsset<T>(path);
}

public GameObject InstantiatePrefab(string path, Transform parent = null)
{
    return BundleManager.EnsureInstance().Instantiate(BuildPrefabPath(path), parent);
}
```

在此基础上，又进一步封装出了：

- `LoadSprite()` / `LoadSpriteAsync()`
- `LoadAudioClip()` / `LoadAudioClipAsync()`
- `LoadPrefab()` / `InstantiatePrefab()`
- `Release()` / `ReleaseInstance()`
- `CheckAndDownloadDependencies()`

这样做的优点是：

- 上层脚本更简洁
- 资源路径规则更统一
- 后续如果底层实现变化，业务层无需大面积修改

### 2.3 常驻管理器模式

`ResourceManager` 与 `BundleManager` 都采用了运行时自动创建的单例模式：

```csharp
public static ResourceManager EnsureInstance()
{
    if (Instance != null)
    {
        return Instance;
    }

    GameObject managerObject = new GameObject("ResourceManager");
    DontDestroyOnLoad(managerObject);
    return managerObject.AddComponent<ResourceManager>();
}
```

这种做法的意义在于：

- 不依赖手动在场景里摆对象
- 不会因为切场景而丢失
- 更适合热更新、资源下载、全局 UI 这类跨场景能力

## 3. 资源热更新（含下载）

### 3.1 设计思路

资源热更新这一块，项目使用的是 Addressables 的远端资源能力。整体流程如下：

```text
启动游戏
    ↓
初始化 Addressables
    ↓
检查 catalog 是否需要更新
    ↓
更新 catalog
    ↓
计算需要下载的资源大小
    ↓
下载远端依赖资源
    ↓
回调 UI 显示下载进度与速度
```

这样做的核心收益是：**资源内容变更时，不需要重新发整包，只需要更新服务器上的 catalog 与 bundle。**

### 3.2 下载状态结构体

为了让下载流程对 UI 更友好，项目定义了 `ResourceDownloadStatus` 作为统一进度结构：

```csharp
public class ResourceDownloadStatus
{
    public string StatusMessage;
    public string CurrentLabel;
    public long DownloadedBytes;
    public long TotalBytes;
    public float Progress;
    public float DownloadBytesPerSecond;
    public bool HasCatalogUpdate;
    public bool IsDone;
}
```

这样资源层只需要不断更新一个数据对象，UI 层就可以直接消费，而不需要知道底层具体用了哪些 Addressables handle。

### 3.3 下载流程实现

在 `BundleManager` 中，下载流程大致分为三段：

#### 第一步：初始化与 catalog 检查

```csharp
AsyncOperationHandle<IResourceLocator> initHandle = Addressables.InitializeAsync(false);
yield return initHandle;

AsyncOperationHandle<List<string>> checkHandle = Addressables.CheckForCatalogUpdates(false);
yield return checkHandle;
```

#### 第二步：统计下载量

```csharp
AsyncOperationHandle<long> sizeHandle = Addressables.GetDownloadSizeAsync(label);
yield return sizeHandle;
```

#### 第三步：执行下载并持续回调进度

```csharp
DownloadStatus downloadStatus = downloadHandle.GetDownloadStatus();
reportedDownloadedBytes = Math.Max(reportedDownloadedBytes, downloadStatus.DownloadedBytes);
ReportProgress(onProgress, progress, "正在下载全部远端资源...", "All remote bundles", reportedDownloadedBytes, totalBytes, downloadBytesPerSecond, hasCatalogUpdate, false);
```

### 3.4 设计上的关键点

这一块有几个比较重要的设计点：

- **先初始化再检查更新**：避免下载前 Addressables 状态不完整
- **先统计大小再下载**：可以提前给玩家显示下载量
- **统一状态回调**：便于 C# UI 和 Lua UI 复用
- **支持 label 下载与全量远端目标下载**：更灵活
- **管理 handle 释放**：避免 Addressables 句柄泄漏

## 4. ToLua 热更新

### 4.1 核心目标

资源热更新解决的是“Prefab、图片、音频等资源”的远端更新问题，但 UI 逻辑和脚本逻辑仍然需要额外手段处理。因此项目又补上了 **Lua 热更新**。

目标是：

- 让 Lua 逻辑可以独立于客户端整包更新
- 尽量不影响原有 ToLua 启动流程
- 通过清单和版本号控制是否需要重新安装 Lua 文件

### 4.2 总体策略

项目没有做“运行中即时替换整个 Lua 虚拟机”的激进方案，而是采用更稳定的方案：

**在 Lua 虚拟机启动之前，先完成 Lua 文件下载和安装；之后 ToLua 启动时优先从热更目录读取脚本。**

这套方案的优点是：

- 简单稳定
- 不容易破坏现有 Lua 生命周期
- 不需要在运行时处理大规模模块替换
- 很适合当前 Demo 项目阶段

### 4.3 启动前更新入口

`LuaHotUpdateManager` 提供了统一的启动入口：

```csharp
public static IEnumerator RunBeforeLuaStartup()
{
    yield return EnsureInstance().RunUpdateWithUICoroutine(DefaultLuaLabels, DefaultManifestAddress);
}
```

也就是说，Lua VM 真正启动前，会先跑完整个更新流程。

### 4.4 下载 + Manifest + 安装

Lua 更新流程核心代码如下：

```csharp
ResourceManager.EnsureInstance().CheckAndDownloadDependencies(
    ParseLabels(labels),
    status =>
    {
        if (status != null)
        {
            UpdateDownloadStatus(status);
        }
    },
    () =>
    {
        downloadFinished = true;
    },
    message =>
    {
        downloadError = message;
        downloadFinished = true;
    });
```

下载完成后，再读取清单：

```csharp
yield return LoadTextAssetContentCoroutine(manifestAddress, content => manifestJson = content);
manifest = JsonUtility.FromJson<LuaHotUpdateManifest>(manifestJson);
```

然后逐个将 Lua 文件安装到本地热更目录：

```csharp
string fullPath = Path.Combine(LuaConst.luaResDir, relativePath);
string directory = Path.GetDirectoryName(fullPath);
if (!string.IsNullOrEmpty(directory))
{
    Directory.CreateDirectory(directory);
}

File.WriteAllBytes(fullPath, luaBytes);
```

### 4.5 为什么需要 Installed Manifest

项目里不是简单地“每次启动都强制重装 Lua”，而是额外保存了一份本地快照：`LuaInstalledManifest.json`。

它的作用有三点：

1. 记录本地已安装的 Lua 版本
2. 判断远端结构是否变化
3. 识别旧文件并清理残留文件

判断是否需要重装时，使用了如下逻辑：

```csharp
if (!string.Equals(installedVersion, manifestVersion, StringComparison.Ordinal))
{
    return true;
}

if (HasManifestStructureChanged(installedManifest, manifest))
{
    return true;
}

return !AreLuaFilesInstalledLocally(manifest);
```

这能解决一个很典型的问题：**远端 bundle 已被 Addressables 缓存，但本地热更 Lua 目录不完整或内容过旧。**

### 4.6 编辑器同步工具

为了减少手工同步 Lua 热更资源的麻烦，项目还实现了 `LuaHotUpdateSyncTool.cs`。

工具入口菜单：

```text
Tools/Lua HotUpdate/Sync Lua To LuaHotUpdate
```

这个工具会自动完成：

- 扫描 `Assets/Lua` 与 `Assets/ToLua/Lua`
- 同步到 `Assets/LuaHotUpdate`
- 将 `.lua` 复制为 `.lua.bytes`
- 生成 `LuaManifest.json`
- 从 `Main.lua` 读取 `VERSION`
- 自动配置 Addressables 资源项
- 自动给 UI prefab 规范 Address

工具里读取版本号的方式也很直接：

```csharp
Match match = Regex.Match(content, "\\bVERSION\\s*=\\s*[\"'](?<version>[^\"']+)[\"']", RegexOptions.IgnoreCase);
return match.Success ? match.Groups["version"].Value.Trim() : "0.0.0";
```

### 4.7 Lua 热更新界面

项目还做了一个启动前的更新 UI：`LuaUpdateUI.prefab` + `LuaUpdateUIView.cs`。

特点包括：

- 启动时自动创建，不需要手动放到场景里
- 支持状态文本、进度条、百分比、大小、速度、重试按钮
- prefab 加载失败时，会自动创建 fallback UI
- 对 TMP 字体做了运行时兜底绑定，避免真机字体丢失导致界面不可见

这套 UI 的意义不只是“好看”，更重要的是：它让热更新流程对玩家是可见、可理解、可重试的。

## 5. 基于 Lua 的 UI 开发框架

### 5.1 为什么要做 Lua UI

如果 UI 全写在 C# 里，那么每次改交互、改文字、改展示节奏都需要重新改 C#、重新打包，效率较低。

因此项目引入 Lua UI 框架，让大量 UI 逻辑迁移到 Lua 层。

设计原则是：

- **C# 负责底层能力和 Unity 对象承载**
- **Lua 负责界面逻辑和业务交互**
- **面板生命周期统一化，降低后续迁移成本**

### 5.2 BasePanel 设计

`BasePanel.lua` 是所有面板的基类，负责：

- 创建和挂载 Prefab
- 保存面板状态
- 初始化 UI 节点和事件
- 控制显示、隐藏和销毁

实例初始化时，会为每个面板统一准备好如下字段：

```lua
local instance = setmetatable({
    visible = false,
    isInited = false,
    data = nil,
    ui = {},
    meta = {},
    state = {},
    definition = nil,
    context = nil,
    panelName = nil,
    prefabPath = nil,
    layer = "Normal",
    showMode = "Replace",
    cache = true,
    isPopup = false,
    closeOnMask = false,
    gameObject = nil,
    transform = nil,
    resourceManager = nil,
}, self)
```

面板显示逻辑如下：

```lua
function BasePanel:Show(data)
    self.visible = true
    self.data = data

    if not self.isInited then
        if self:CreateGameObject() == nil then
            self.visible = false
            return
        end

        self:InitUIAndMetaData()
        self:InitUIEvent()
        self.isInited = true
    end

    self:OnOpen(data)
    self:RefreshView(data)
end
```

这就把所有面板统一成同一套生命周期：

- `Ctor`
- `InitUIAndMetaData`
- `InitUIEvent`
- `OnOpen`
- `RefreshView`
- `OnHide`
- `OnDispose`

### 5.3 UIPanelManager 设计

`UIPanelManager.lua` 负责更高层的管理，包括：

- 面板定义注册
- 面板实例创建
- 打开/关闭/返回/销毁
- 面板栈管理
- 层级根节点管理
- 可见面板的逐帧更新

核心打开逻辑：

```lua
function UIPanelManager.Open(name, data)
    local panel = panelInstances[name]
    if panel == nil then
        panel = createPanel(name)
        panelInstances[name] = panel
    end

    local showMode = panel.showMode or definition.showMode or DefaultShowMode
    if showMode ~= OverlayShowMode then
        hidePanel(findTopVisiblePanelName(name))
    end

    panel:Show(data)
    removeFromStack(name)
    table.insert(panelStack, name)
    return panel
end
```

这意味着：

- 默认打开新界面时，会隐藏之前的顶层界面
- 支持 Overlay 类型面板叠加显示
- 支持缓存型与非缓存型面板
- 关闭顶层面板后可以自动恢复前一个面板

### 5.4 场景化与跨场景问题

后来在实际迁移过程中，又遇到了几个非常关键的问题：

- 切到 `Level1` 后 Lua VM 被卸载
- 场景里重复出现 `LuaClient`
- 非启动场景错误弹出 `StartPanel`
- 某些 UI 持有旧场景节点引用

针对这些问题，项目做了几项架构修正：

- 让 `LuaClient` 常驻并单例防重
- 启动 UI 按当前场景决定是否显示，而不是无条件打开
- 切场景时销毁场景绑定型 Panel，防止旧节点引用残留

这部分修正的意义很大：没有它，即使 Lua UI 框架本身成立，实际运行时也会因为跨场景生命周期问题而不稳定。

### 5.5 教学 UI 的迁移方式

`TutorialUI` 是一个比较特殊的例子，因为它已经被多个场景引用。

因此这里没有简单粗暴地重做 Prefab，而是采用了 **桥接兼容** 的办法：

- C# 保留 `TutorialUI.instance`
- 保留 `ShowTutorial(...)` / `HideTutorial(...)`
- 优先转发给 Lua 的 `TutorialPanelBridge`
- Lua 未就绪时，继续走 C# fallback

这是一种很实用的渐进式迁移思路：**不是所有旧系统都一次性推翻，而是先保留兼容层，再逐步替换。**

## 6. 对象池系统

### 6.1 为什么要做对象池

在动作游戏里，有三类对象特别容易产生频繁的创建和销毁：

- 怪物
- 短生命周期特效
- 投射物 / 行为对象

如果每次都 `Instantiate` / `Destroy`，很容易带来额外开销与不稳定性。因此项目把对象池拆成了三套：

- `EnemyPoolManager`
- `EffectPoolManager`
- `ProjectilePoolManager`

### 6.2 怪物对象池

怪物对象池比较复杂，因为怪物不是“播完就没”的短对象，而是带 AI、受击、死亡、复活流程的实体。

因此它需要额外管理：

- 活跃对象集合
- 非活跃对象队列
- 按场景清理
- 死亡后延迟复活
- 原始刷怪点与刷怪 ID

生成逻辑：

```csharp
public GameObject Spawn(string prefabPath, Vector3 position, Quaternion rotation, Transform parent = null)
{
    PooledEnemy pooledEnemy = Acquire(prefabPath);
    Transform enemyTransform = pooledEnemy.transform;
    enemyTransform.SetParent(parent, false);
    enemyTransform.position = position;
    enemyTransform.rotation = rotation;
    pooledEnemy.RecordSpawnContext(position, rotation, parent);
    pooledEnemy.gameObject.SetActive(true);
    pooledEnemy.NotifyAfterSpawn();
    activeEnemies.Add(pooledEnemy);
    return pooledEnemy.gameObject;
}
```

回收逻辑：

```csharp
internal void Despawn(PooledEnemy pooledEnemy)
{
    activeEnemies.Remove(pooledEnemy);
    pooledEnemy.NotifyBeforeDespawn();
    pooledEnemy.transform.SetParent(transform, false);
    pooledEnemy.gameObject.SetActive(false);
    GetQueue(pooledEnemy.PoolKey).Enqueue(pooledEnemy);
}
```

### 6.3 配置化刷怪

怪物对象池没有停留在“池管理器”层面，而是已经和场景刷怪逻辑接上了。

`EnemySpawnBootstrap.cs` 会在场景加载后：

1. 从 Lua 读取怪物模板表 `MMonsterData`
2. 按场景名读取刷怪表 `M<SceneName>MonsterSpawnData`
3. 预热对象池
4. 隐藏旧的手摆怪
5. 按配置表批量生成怪物

读取场景表的代码：

```csharp
string sceneSpawnTableName = GetSceneSpawnTableName(sceneName);
string sceneSpawnModulePath = $"Data.{sceneSpawnTableName}";
luaState.DoString(
    $"{SceneSpawnTablePath} = require '{sceneSpawnModulePath}'",
    $"EnemySpawnBootstrap_{sceneSpawnTableName}");
```

这意味着怪物的以下内容都可以配置化：

- 类型
- 刷新位置
- 巡逻范围
- 朝向
- 预热数量
- 血量、移动速度等参数
- 死亡尸体停留时间
- 复活时间

对后续关卡扩展非常有帮助。

### 6.4 特效对象池

特效对象池 `EffectPoolManager` 主要服务于：

- 受击特效
- 剑命中特效
- 跳跃灰尘
- 爆炸粒子
- Boss 战中的高频短效

接口设计比较轻量：

```csharp
public static GameObject Play(GameObject effectPrefab, Vector3 position, Quaternion rotation, Transform parent = null)
{
    if (effectPrefab == null || IsShuttingDown)
    {
        return null;
    }

    EffectPoolManager manager = EnsureInstance();
    return manager != null ? manager.Spawn(effectPrefab, position, rotation, parent) : null;
}
```

它的关键特点是：

- 支持按 prefab 实例 ID 建池
- 支持首次动态补挂 `PooledEffect`
- 场景切换时统一清空
- 退出应用时避免错误回池

### 6.5 投射物对象池

投射物对象池和特效对象池很像，但它服务的是“有行为”的对象，比如：

- 玩家火球
- FalseKnight 冲击波

之所以单独拆一套，是因为这类对象往往带：

- 速度
- 生命周期计时
- 命中逻辑
- 出池初始化
- 回池前复位

这样拆开后，职责更清晰，也更便于后续扩展其它投射物。

## 7. 存档系统

### 7.1 存档需求

项目的存档系统需要解决的是：

- 有多个存档槽位
- 能从主菜单查看存档列表
- 能继续最近存档
- 能新开游戏、覆盖旧档、删除存档
- 读档后正确恢复场景和玩家状态

### 7.2 数据结构设计

项目采用 JSON 文件存档，核心数据结构为 `SaveData`：

```csharp
[Serializable]
private class SaveData
{
    public int version;
    public int slotId;
    public string sceneName;
    public string locationName;
    public string iconName;
    public float playerX;
    public float playerY;
    public float playerZ;
    public float respawnX;
    public float respawnY;
    public float respawnZ;
    public int currentHealth;
    public int maxHealth;
    public float currentSoulPower;
    public float maxSoulPower;
    public float playTimeSeconds;
    public string saveTime;
}
```

这套结构已经能覆盖目前 Demo 所需的大部分信息。

### 7.3 写档流程

写档时，会收集当前玩家对象以及相关组件：

- `PlayerHealth`
- `PlayerSoulPower`
- `PlayerController`
- 当前场景名
- 当前安全点

然后统一序列化成 JSON：

```csharp
Directory.CreateDirectory(GetSaveFolderPath());
File.WriteAllText(GetSaveFilePath(slotId), JsonUtility.ToJson(data, true));
SetCurrentSlot(slotId);
```

存档目录位于：

```text
Application.persistentDataPath/Save
```

### 7.4 读档流程

读档不能简单地“当前场景里直接改状态”，而是需要：

1. 先读取存档文件
2. 加载存档记录的场景
3. 等场景完全载入后，再恢复玩家状态

项目中采用了 `SceneManager.sceneLoaded + 下一帧协程` 的方式处理：

```csharp
private void LoadSlot(int slotId)
{
    pendingLoadData = ReadSlot(slotId);
    SceneManager.sceneLoaded -= OnLoadSceneForSave;
    SceneManager.sceneLoaded += OnLoadSceneForSave;
    SceneManager.LoadScene(pendingLoadData.sceneName);
}
```

场景载入后：

```csharp
player.transform.position = playerPosition;
rb2d.velocity = Vector2.zero;
rb2d.angularVelocity = 0f;
rb2d.simulated = true;

playerHealth.ApplySaveData(pendingLoadData.currentHealth, pendingLoadData.maxHealth, respawnPosition);
playerSoulPower.SetSoulPower(pendingLoadData.currentSoulPower, pendingLoadData.maxSoulPower);
```

这样可以确保场景对象已经存在，再恢复状态，避免空引用或初始化顺序错误。

### 7.5 UI 交互层

存档系统并不是独立存在的，而是和 Lua UI 打通了。主菜单中的：

- 开始游戏
- 继续游戏
- 存档列表展示
- 删除存档确认弹窗

都已经接到 Lua Panel 上。

这说明存档系统已经不仅仅是底层能力，而是完整参与了玩家的实际流程。

## 8. 关键问题与修复经验

这一阶段除了新功能，还有不少值得记录的修复经验。

### 8.1 Lua VM 常驻与跨场景稳定性

问题：切场景后 Lua VM 丢失，或者重复创建新的 LuaClient。

解决：

- `LuaClient` 改为单例防重
- `DontDestroyOnLoad`
- 启动场景 UI 按场景名决定是否显示
- 场景切换时清理场景绑定型 Panel

### 8.2 UI Prefab Address 规范化

问题：真机环境下 UI prefab 的 Address 与代码请求值不一致，导致加载失败。

解决：在同步工具中自动扫描 `Assets/Prefab/UI`，统一生成 `UI/xxx` 格式的 Address，并清理文件夹级错误条目。

### 8.3 TMP 字体问题

问题：真机包里 `LuaUpdateUI` 的 `TextMeshProUGUI` 没有绑定字体，出现紫色块或不可见。

解决：

- 设置 TMP 默认字体
- fallback UI 运行时给文本显式绑定字体
- prefab 版更新 UI 实例化后整棵文本树统一补字体

### 8.4 TutorialUI 空指针

问题：真机退出教程触发区时，`TutorialUI` 的 fallback 通过子节点顺序缓存教程对象，某些节点缺失或顺序变化就可能导致空指针。

解决：

- 改为按固定节点名绑定
- 增加元素级空判断
- 触发器侧增加 `TutorialUI.instance` 判空

这个修复很有代表性：**很多编辑器里不容易暴露的问题，往往会在真机或打包环境下被放大，因此运行时保护非常重要。**

## 9. 这一阶段的收获

回头看这一阶段，最大的收获不是单个功能点，而是项目的整体工程能力提升了：

- 资源有统一入口了
- 远端资源和 Lua 都能热更新了
- UI 有了成体系的 Lua 框架
- 高频对象复用更稳定了
- 存档流程闭环了
- 真机环境下暴露的问题也逐步被修掉了

这些能力共同作用，才让项目从“能演示玩法”走向“能持续迭代、能稳定展示、能继续扩展”。

后续如果继续深入，我认为最值得继续完善的方向有：

- 更多 UI 迁移到 Lua Panel
- 更完整的 Lua 模块热重载能力
- 更完善的资源分组与版本发布流程
- 更系统的配置表驱动能力
- 更多对象池和行为对象的统一规范

这份总结的价值就在于：以后无论是继续开发这个项目，还是在新的 Unity 项目里复用这些思路，都可以直接拿来参考。